// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for SawmillModule
#include "SawmillModule.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* SawmillModule_preinit() is required to be called to 
 * initialise an instance of SawmillModule. 
 * It sets all I/O values to zero.
 */
int SawmillModule_preinit(SawmillModule_t  *me) {
	//if there are input events, reset them
	
	//if there are output events, reset them
	me->outputEvents.event.MessageChange = 0;
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Led_preinit(&me->saw) != 0) {
		return 1;
	}
	if(SawmillControl_preinit(&me->control) != 0) {
		return 1;
	}
	if(MaskedSwitch_preinit(&me->runswitch) != 0) {
		return 1;
	}
	if(MaskedSwitch_preinit(&me->scale) != 0) {
		return 1;
	}
	if(MaskedSwitch_preinit(&me->laser) != 0) {
		return 1;
	}
	if(SawmillSpeed_preinit(&me->speedmon) != 0) {
		return 1;
	}
	if(SawmillPosGen_preinit(&me->posgen) != 0) {
		return 1;
	}
	if(MaskedSwitch_preinit(&me->stall) != 0) {
		return 1;
	}
	
	
	//if this is a BFB, set _trigger to be true and start state so that the start state is properly executed
	

	return 0;
}

/* SawmillModule_init() is required to be called to 
 * set up an instance of SawmillModule. 
 * It passes around configuration data.
 */
int SawmillModule_init(SawmillModule_t  *me) {
	//pass in any parameters on this level
	me->runswitch.SwitchMask = 0b0001;
	me->scale.SwitchMask = 0b0010;
	me->laser.SwitchMask = 0b0100;
	me->stall.SwitchMask = 0b1000;
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	me->Message = me->control.Message;
	me->saw.Run = me->control.SawRun;
	me->posgen.SawRun = me->control.SawRun;
	me->control.ControlRun = me->runswitch.SwitchOn;
	me->control.ScaleOverweight = me->scale.SwitchOn;
	me->control.LaserBroken = me->laser.SwitchOn;
	me->control.BadSpeed = me->speedmon.BadSpeed;
	me->speedmon.Pos = me->posgen.NewPos;
	me->control.StallDetected = me->stall.SwitchOn;
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Led_init(&me->saw) != 0) {
		return 1;
	}
	if(SawmillControl_init(&me->control) != 0) {
		return 1;
	}
	if(MaskedSwitch_init(&me->runswitch) != 0) {
		return 1;
	}
	if(MaskedSwitch_init(&me->scale) != 0) {
		return 1;
	}
	if(MaskedSwitch_init(&me->laser) != 0) {
		return 1;
	}
	if(SawmillSpeed_init(&me->speedmon) != 0) {
		return 1;
	}
	if(SawmillPosGen_init(&me->posgen) != 0) {
		return 1;
	}
	if(MaskedSwitch_init(&me->stall) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* SawmillModule_syncOutputEvents() synchronises the output events of an
 * instance of SawmillModule as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void SawmillModule_syncOutputEvents(SawmillModule_t  *me) {
	//first, for all cfb children, call this same function
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
	me->outputEvents.event.MessageChange = me->control.outputEvents.event.MessageChange; 
	
}

/* SawmillModule_syncInputEvents() synchronises the input events of an
 * instance of SawmillModule as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void SawmillModule_syncInputEvents(SawmillModule_t  *me) {
	//first, we explicitly synchronise the children
	
	me->saw.inputEvents.event.CommandChange = me->control.outputEvents.event.CommandChange; 
	
	me->control.inputEvents.event.ControlChange = me->runswitch.outputEvents.event.SwitchChanged; 
	
	me->control.inputEvents.event.WeightChange = me->scale.outputEvents.event.SwitchChanged; 
	
	me->control.inputEvents.event.LaserChange = me->laser.outputEvents.event.SwitchChanged; 
	
	me->control.inputEvents.event.BadSpeedChange = me->speedmon.outputEvents.event.BadSpeedChange; 
	
	me->control.inputEvents.event.StallDetectedChange = me->stall.outputEvents.event.SwitchChanged; 
	
	me->speedmon.inputEvents.event.NewPos = me->posgen.outputEvents.event.NewPosChange; 
	
	me->posgen.inputEvents.event.SawRunChange = me->control.outputEvents.event.CommandChange; 
	

	//then, call this same function on all cfb children
	
}

/* SawmillModule_syncOutputData() synchronises the output data connections of an
 * instance of SawmillModule as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void SawmillModule_syncOutputData(SawmillModule_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	me->Message = me->control.Message;
	
	
}

/* SawmillModule_syncInputData() synchronises the input data connections of an
 * instance of SawmillModule as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void SawmillModule_syncInputData(SawmillModule_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for saw (of type Led) which is a BFB
	
	if(me->saw.inputEvents.event.CommandChange == 1) { 
		me->saw.Run = me->control.SawRun;
	} 
	
	//sync for control (of type SawmillControl) which is a BFB
	
	if(me->control.inputEvents.event.ControlChange == 1) { 
		me->control.ControlRun = me->runswitch.SwitchOn;
	} 
	if(me->control.inputEvents.event.WeightChange == 1) { 
		me->control.ScaleOverweight = me->scale.SwitchOn;
	} 
	if(me->control.inputEvents.event.LaserChange == 1) { 
		me->control.LaserBroken = me->laser.SwitchOn;
	} 
	if(me->control.inputEvents.event.BadSpeedChange == 1) { 
		me->control.BadSpeed = me->speedmon.BadSpeed;
	} 
	if(me->control.inputEvents.event.StallDetectedChange == 1) { 
		me->control.StallDetected = me->stall.SwitchOn;
	} 
	
	//sync for runswitch (of type MaskedSwitch) which is a BFB
	
	
	//sync for scale (of type MaskedSwitch) which is a BFB
	
	
	//sync for laser (of type MaskedSwitch) which is a BFB
	
	
	//sync for speedmon (of type SawmillSpeed) which is a BFB
	
	if(me->speedmon.inputEvents.event.NewPos == 1) { 
		me->speedmon.Pos = me->posgen.NewPos;
	} 
	
	//sync for posgen (of type SawmillPosGen) which is a BFB
	
	if(me->posgen.inputEvents.event.SawRunChange == 1) { 
		me->posgen.SawRun = me->control.SawRun;
	} 
	
	//sync for stall (of type MaskedSwitch) which is a BFB
	
	
	
	//for all composite function block children, call this same function
	
	
}


/* SawmillModule_run() executes a single tick of an
 * instance of SawmillModule according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void SawmillModule_run(SawmillModule_t  *me) {
	Led_run(&me->saw);
	SawmillControl_run(&me->control);
	MaskedSwitch_run(&me->runswitch);
	MaskedSwitch_run(&me->scale);
	MaskedSwitch_run(&me->laser);
	SawmillSpeed_run(&me->speedmon);
	SawmillPosGen_run(&me->posgen);
	MaskedSwitch_run(&me->stall);
	
}


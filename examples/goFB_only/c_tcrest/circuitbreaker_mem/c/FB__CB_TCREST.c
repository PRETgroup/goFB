// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Device Function Block for _CB_TCREST
#include "FB__CB_TCREST.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;



/* _CB_TCREST_preinit() is required to be called to 
 * initialise an instance of _CB_TCREST. 
 * It sets all I/O values to zero.
 */
int _CB_TCREST_preinit(_CB_TCREST_t  *me) {
	

	
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	
	
	//set any resource params
	
	
	if(_CBCore0_preinit(&me->Resource) != 0) {
		return 1;
	}
	
	if(_CBCore1_preinit(&me->Resource1) != 0) {
		return 1;
	}
	
	if(_CBCore2_preinit(&me->Resource2) != 0) {
		return 1;
	}
	
	if(_CBCore3_preinit(&me->Resource3) != 0) {
		return 1;
	}
	
	

	
	
	

	

	return 0;
}

/* _CB_TCREST_init() is required to be called to 
 * set up an instance of _CB_TCREST. 
 * It passes around configuration data.
 */
int _CB_TCREST_init(_CB_TCREST_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	
	if(_CBCore0_init(&me->Resource) != 0) {
		return 1;
	}
	if(_CBCore1_init(&me->Resource1) != 0) {
		return 1;
	}
	if(_CBCore2_init(&me->Resource2) != 0) {
		return 1;
	}
	if(_CBCore3_init(&me->Resource3) != 0) {
		return 1;
	}
	

	return 0;
}



/* _CB_TCREST_sync[Output/Input]Events() synchronises the events of an
 * instance of _CB_TCREST as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _CB_TCREST_syncOutputEvents(_CB_TCREST_t  *me) {
	//for all device function block resource function blocks, call this same function
	//resources are the only things that can be embedded in devices
	//sync for Resource (of type _CBCore0) which is a Resource
	_CBCore0_syncOutputEvents(&me->Resource);//sync for Resource1 (of type _CBCore1) which is a Resource
	_CBCore1_syncOutputEvents(&me->Resource1);//sync for Resource2 (of type _CBCore2) which is a Resource
	_CBCore2_syncOutputEvents(&me->Resource2);//sync for Resource3 (of type _CBCore3) which is a Resource
	_CBCore3_syncOutputEvents(&me->Resource3);
	
}
void _CB_TCREST_syncInputEvents(_CB_TCREST_t  *me) {
	//for all device function block resource function blocks, call this same function
	//resources are the only things that can be embedded in devices
	//sync for Resource (of type _CBCore0) which is a Resource
	_CBCore0_syncInputEvents(&me->Resource);//sync for Resource1 (of type _CBCore1) which is a Resource
	_CBCore1_syncInputEvents(&me->Resource1);//sync for Resource2 (of type _CBCore2) which is a Resource
	_CBCore2_syncInputEvents(&me->Resource2);//sync for Resource3 (of type _CBCore3) which is a Resource
	_CBCore3_syncInputEvents(&me->Resource3);
	
}

/* _CB_TCREST_sync[Output/Input]Data() synchronises the data connections of an
 * instance of _CB_TCREST as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _CB_TCREST_syncOutputData(_CB_TCREST_t  *me) {
	//for all device function block resource function blocks, call this same function
	//resources are the only things that can be embedded in devices
	//sync for Resource (of type _CBCore0) which is a Resource
	_CBCore0_syncOutputData(&me->Resource);//sync for Resource1 (of type _CBCore1) which is a Resource
	_CBCore1_syncOutputData(&me->Resource1);//sync for Resource2 (of type _CBCore2) which is a Resource
	_CBCore2_syncOutputData(&me->Resource2);//sync for Resource3 (of type _CBCore3) which is a Resource
	_CBCore3_syncOutputData(&me->Resource3);

}
void _CB_TCREST_syncInputData(_CB_TCREST_t  *me) {
	//for all device function block resource function blocks, call this same function
	//resources are the only things that can be embedded in devices
	//sync for Resource (of type _CBCore0) which is a Resource
	_CBCore0_syncInputData(&me->Resource);//sync for Resource1 (of type _CBCore1) which is a Resource
	_CBCore1_syncInputData(&me->Resource1);//sync for Resource2 (of type _CBCore2) which is a Resource
	_CBCore2_syncInputData(&me->Resource2);//sync for Resource3 (of type _CBCore3) which is a Resource
	_CBCore3_syncInputData(&me->Resource3);

}


/* _CB_TCREST_run() executes a single tick of an
 * instance of _CB_TCREST according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void _CB_TCREST_run(_CB_TCREST_t  *me) {
	_CBCore0_run(&me->Resource);
	_CBCore1_run(&me->Resource1);
	_CBCore2_run(&me->Resource2);
	_CBCore3_run(&me->Resource3);
	
}


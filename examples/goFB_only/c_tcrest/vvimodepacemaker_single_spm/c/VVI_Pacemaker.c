// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for VVI_Pacemaker
#include "VVI_Pacemaker.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* VVI_Pacemaker_preinit() is required to be called to 
 * initialise an instance of VVI_Pacemaker. 
 * It sets all I/O values to zero.
 */
int VVI_Pacemaker_preinit(VVI_Pacemaker_t _SPM *me) {
	//if there are input events, reset them
	me->inputEvents.event.VPulse = 0;
	me->inputEvents.event.VRP_Timer_Timeout = 0;
	me->inputEvents.event.LRI_Timer_Timeout = 0;
	
	//if there are output events, reset them
	me->outputEvents.event.VPace = 0;
	me->outputEvents.event.VRefractory = 0;
	me->outputEvents.event.VRP_Start_Timer = 0;
	me->outputEvents.event.LRI_Timer_Start = 0;
	me->outputEvents.event.LRI_Timer_Stop = 0;
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(VVI_LRI_preinit(&me->VVI_LRI) != 0) {
		return 1;
	}
	if(VVI_VRP_preinit(&me->VVI_VRP) != 0) {
		return 1;
	}
	
	
	//if this is a BFB, set _trigger to be true and start state so that the start state is properly executed
	

	return 0;
}

/* VVI_Pacemaker_init() is required to be called to 
 * set up an instance of VVI_Pacemaker. 
 * It passes around configuration data.
 */
int VVI_Pacemaker_init(VVI_Pacemaker_t _SPM *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	me->VRP_Timeout_Value = me->VVI_VRP.VRP_Timeout_Value;
	me->LRI_Timeout_Value = me->VVI_LRI.LRI_Timeout_Value;
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(VVI_LRI_init(&me->VVI_LRI) != 0) {
		return 1;
	}
	if(VVI_VRP_init(&me->VVI_VRP) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* VVI_Pacemaker_syncOutputEvents() synchronises the output events of an
 * instance of VVI_Pacemaker as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void VVI_Pacemaker_syncOutputEvents(VVI_Pacemaker_t _SPM *me) {
	//first, for all cfb children, call this same function
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
	me->outputEvents.event.VPace = me->VVI_LRI.outputEvents.event.VPace; 
	
	me->outputEvents.event.VRefractory = me->VVI_VRP.outputEvents.event.VRefractory; 
	
	me->outputEvents.event.VRP_Start_Timer = me->VVI_VRP.outputEvents.event.VRP_Start_Timer; 
	
	me->outputEvents.event.LRI_Timer_Start = me->VVI_LRI.outputEvents.event.LRI_Timer_Start; 
	
	me->outputEvents.event.LRI_Timer_Stop = me->VVI_LRI.outputEvents.event.LRI_Timer_Stop; 
	
}

/* VVI_Pacemaker_syncInputEvents() synchronises the input events of an
 * instance of VVI_Pacemaker as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void VVI_Pacemaker_syncInputEvents(VVI_Pacemaker_t _SPM *me) {
	//first, we explicitly synchronise the children
	
	me->VVI_LRI.inputEvents.event.VSense = me->VVI_VRP.outputEvents.event.VSense; 
	
	me->VVI_LRI.inputEvents.event.LRI_Timer_Timeout = me->inputEvents.event.LRI_Timer_Timeout; 
	
	me->VVI_VRP.inputEvents.event.VPulse = me->inputEvents.event.VPulse; 
	
	me->VVI_VRP.inputEvents.event.VPace = me->VVI_LRI.outputEvents.event.VPace; 
	
	me->VVI_VRP.inputEvents.event.VRP_Timer_Timeout = me->inputEvents.event.VRP_Timer_Timeout; 
	

	//then, call this same function on all cfb children
	
}

/* VVI_Pacemaker_syncOutputData() synchronises the output data connections of an
 * instance of VVI_Pacemaker as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void VVI_Pacemaker_syncOutputData(VVI_Pacemaker_t _SPM *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	me->VRP_Timeout_Value = me->VVI_VRP.VRP_Timeout_Value;
	me->LRI_Timeout_Value = me->VVI_LRI.LRI_Timeout_Value;
	
	
}

/* VVI_Pacemaker_syncInputData() synchronises the input data connections of an
 * instance of VVI_Pacemaker as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void VVI_Pacemaker_syncInputData(VVI_Pacemaker_t _SPM *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for VVI_LRI (of type VVI_LRI) which is a BFB
	
	
	//sync for VVI_VRP (of type VVI_VRP) which is a BFB
	
	
	
	//for all composite function block children, call this same function
	
	
}


/* VVI_Pacemaker_run() executes a single tick of an
 * instance of VVI_Pacemaker according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void VVI_Pacemaker_run(VVI_Pacemaker_t _SPM *me) {
	VVI_LRI_run(&me->VVI_LRI);
	VVI_VRP_run(&me->VVI_VRP);
	
}


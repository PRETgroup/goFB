// This file is generated by FBC.

#include "SawmillMessageHandler.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void SawmillMessageHandlerinit(SawmillMessageHandler* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
}

/* ECC algorithms */
void SawmillMessageHandler_LoadData(SawmillMessageHandler* me)
{
me->TxData = me->Message;
}

/* Function block execution function */
void SawmillMessageHandlerrun(SawmillMessageHandler* me)
{
    me->_output.events = 0;

    if (me->_input.events) {
        if (me->_input.event.MessageChanged) {
            me->Message = me->_Message;
        }
        if (me->_input.event.TxSuccessChanged) {
            me->TxSuccess = me->_TxSuccess;
        }
    }
    #pragma loopbound min 1 max 2
    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: tx_done
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.MessageChanged) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: try_tx
                if (!me->_entered) {
                    SawmillMessageHandler_LoadData(me);
                    me->_output.event.TxDataPresent = 1;
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.TxSuccessChanged && (! me->TxSuccess)) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                    else if (me->_input.event.TxSuccessChanged && (me->TxSuccess)) {
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
        }
        break;
    }
    if (me->_output.event.TxDataPresent) {
        me->_TxData = me->TxData;
    }

    me->_input.events = 0;
}


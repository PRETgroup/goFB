// This file is generated by FBC.

#include "Print.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void Printinit(Print* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
}

/* ECC algorithms */
void Print_PrintService(Print* me)
{
//printf("%lu\n", me->Count);
HEX = (unsigned int)me->Count;
}

/* Function block execution function */
void Printrun(Print* me)
{
    
    if (me->_input.event.CountChanged) {
        me->Count = me->_Count;
    }
	#pragma loopbound min 1 max 2
    for (;;) {
        if (me->_state == 0) {
            // State: Start
            if (!me->_entered) {
                me->_entered = true;
            }
            else {
                if (me->_input.event.CountChanged) {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
            }
        }
        else {
            // State: print
            if (!me->_entered) {
                Print_PrintService(me);
                me->_entered = true;
            }
            else {
                if (me->_input.event.CountChanged) {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
            }
        }
        break;
    }
    me->_input.event.CountChanged = 0;
}


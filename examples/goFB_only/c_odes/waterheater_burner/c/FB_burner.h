// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block burner
#ifndef BURNER_H_
#define BURNER_H_

#include "fbtypes.h"
#include "util.h"


#include "cvode/cvode.h"
#include "nvector/nvector_serial.h"
#include "cvode/cvode_dense.h"
#include "sundials/sundials_dense.h"
#include "sundials/sundials_types.h"


//This is a BFB, so we need an enum type for the state machine
enum burner_states { STATE_burner_Start, STATE_burner_State1E0, STATE_burner_State1, STATE_burner_State2E0, STATE_burner_State2, STATE_burner_State3E0, STATE_burner_State3, STATE_burner_State4E0, STATE_burner_State4, STATE_burner_State1E1 };


union burnerInputEvents {
	struct {
		UDINT TURNON;
		UDINT TURNOFF;
	} event;
	
};


union burnerOutputEvents {
	struct {
		UDINT update;
		UDINT ON;
		UDINT OFF;
	} event;
	
};


typedef struct {
    //input events
	union burnerInputEvents inputEvents;

    //output events
	union burnerOutputEvents outputEvents;

    //input vars
	LREAL DeltaTime;
    
    //output vars
	LREAL y;
    
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	enum burner_states _state; //stores current state
	BOOL _trigger; //indicates if a state transition has occured this tick
	//this block uses cvode
	void *cvode_mem;
	N_Vector ode_solution;
	realtype T0;
	realtype Tnext;
	realtype Tcurr;
	int solveInProgress;
	

	
} burner_t;

//all FBs get a preinit function
int burner_preinit(burner_t  *me);

//all FBs get an init function
int burner_init(burner_t  *me);

//all FBs get a run function
void burner_run(burner_t  *me);



#endif

// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Basic Function Block for controller
#include "FB_controller.h"


/* controller_preinit() is required to be called to 
 * initialise an instance of controller. 
 * It sets all I/O values to zero.
 */
int controller_preinit(controller_t  *me) {
	//if there are input events, reset them
	me->inputEvents.event.update = 0;
	
	//if there are output events, reset them
	me->outputEvents.event.add1 = 0;
	me->outputEvents.event.add2 = 0;
	me->outputEvents.event.remove1 = 0;
	me->outputEvents.event.remove2 = 0;
	
	//if there are input vars with default values, set them
	me->DeltaTime = 0.01;
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	me->ctime = 0;
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	
	
	//if this is a BFB/odeFB, set start state so that the start state is properly executed and _trigger if necessary
	me->_state = STATE_controller_Start;
	me->_trigger = true;
	
	
	

	return 0;
}

/* controller_init() is required to be called to 
 * set up an instance of controller. 
 * It passes around configuration data.
 */
int controller_init(controller_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	
	

	return 0;
}



//algorithms

void controller_resettime(controller_t  *me) {
//PROVIDED CODE: this algorithm was provided in an algorithm's text field
me->ctime = 0;
}

void controller_addtime(controller_t  *me) {
//PROVIDED CODE: this algorithm was provided in an algorithm's text field
me->ctime += me->DeltaTime;
}



/* controller_run() executes a single tick of an
 * instance of controller according to synchronous semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * will need to be done in the parent.
 * Also note that on the first run of this function, trigger will be set
 * to true, meaning that on the very first run no next state logic will occur.
 */
void controller_run(controller_t  *me) {
	//if there are output events, reset them
	
	me->outputEvents.event.add1 = 0;
	me->outputEvents.event.add2 = 0;
	me->outputEvents.event.remove1 = 0;
	me->outputEvents.event.remove2 = 0;
	

	//next state logic
	if(me->_trigger == false) {
		switch(me->_state) {
		case STATE_controller_Start:
			if(true) {
				me->_state = STATE_controller_State1;
				me->_trigger = true;
			};
			break;
		case STATE_controller_State1:
			if(LREAL_GTE(me->x, 550) || me->ctime >= 20) {
				me->_state = STATE_controller_State2;
				me->_trigger = true;
			} else if(true) {
				me->_state = STATE_controller_State1;
				me->_trigger = true;
			};
			break;
		case STATE_controller_State2:
			if(LREAL_LTE(me->x, 510)) {
				me->_state = STATE_controller_State3;
				me->_trigger = true;
			};
			break;
		case STATE_controller_State3:
			if(true) {
				me->_state = STATE_controller_State4;
				me->_trigger = true;
			};
			break;
		case STATE_controller_State4:
			if(LREAL_GTE(me->x, 550) || me->ctime >= 20) {
				me->_state = STATE_controller_State5;
				me->_trigger = true;
			} else if(true) {
				me->_state = STATE_controller_State4;
				me->_trigger = true;
			};
			break;
		case STATE_controller_State5:
			if(LREAL_LTE(me->x, 510)) {
				me->_state = STATE_controller_State6;
				me->_trigger = true;
			};
			break;
		case STATE_controller_State6:
			if(true) {
				me->_state = STATE_controller_State1;
				me->_trigger = true;
			};
			break;
		
		}
	}

	//state output logic
	if(me->_trigger == true) {
		switch(me->_state) {
		case STATE_controller_Start:
			break;

		case STATE_controller_State1:
			controller_addtime(me);
			break;

		case STATE_controller_State2:
			me->outputEvents.event.add1 = 1;
			break;

		case STATE_controller_State3:
			controller_resettime(me);
			me->outputEvents.event.remove1 = 1;
			break;

		case STATE_controller_State4:
			controller_addtime(me);
			break;

		case STATE_controller_State5:
			me->outputEvents.event.add2 = 1;
			break;

		case STATE_controller_State6:
			controller_resettime(me);
			me->outputEvents.event.remove2 = 1;
			break;

		
		}
	}

	me->_trigger = false;
}



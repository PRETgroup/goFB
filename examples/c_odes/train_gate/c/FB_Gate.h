// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block Gate
#ifndef GATE_H_
#define GATE_H_

#include "fbtypes.h"
#include "util.h"


#include "cvode/cvode.h"
#include "nvector/nvector_serial.h"
#include "cvode/cvode_dense.h"
#include "sundials/sundials_dense.h"
#include "sundials/sundials_types.h"


//This is a BFB, so we need an enum type for the state machine
enum Gate_states { STATE_Gate_Start, STATE_Gate_State2E2, STATE_Gate_State1, STATE_Gate_State2E0, STATE_Gate_State2, STATE_Gate_State1E1 };


union GateInputEvents {
	struct {
		UDINT UP;
		UDINT DOWN;
	} event;
	
};


union GateOutputEvents {
	struct {
		UDINT update_g;
	} event;
	
};


typedef struct {
    //input events
	union GateInputEvents inputEvents;

    //output events
	union GateOutputEvents outputEvents;

    //input vars
	LREAL DeltaTime;
    
    //output vars
	LREAL x;
    
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	enum Gate_states _state; //stores current state
	BOOL _trigger; //indicates if a state transition has occured this tick
	//this block uses cvode
	void *cvode_mem;
	N_Vector ode_solution;
	realtype T0;
	realtype Tnext;
	realtype Tcurr;
	int solveInProgress;
	

	
} Gate_t;

//all FBs get a preinit function
int Gate_preinit(Gate_t  *me);

//all FBs get an init function
int Gate_init(Gate_t  *me);

//all FBs get a run function
void Gate_run(Gate_t  *me);



#endif

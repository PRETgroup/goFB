// This file has been automatically generated by goFB
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

//This is the main file for the iec61499 network with _pizza as the top level block

#include "FB__pizza.h"

//put a copy of the top level block into global memory
_pizza_t my_pizza;

int main() {
	printf("\n\n\n");

	if(_pizza_preinit(&my_pizza) != 0) {
		printf("Failed to preinitialize.");
		return 1;
	}
	if(_pizza_init(&my_pizza) != 0) {
		printf("Failed to initialize.");
		return 1;
	}

	printf("Top: %20s   Size: %lu\n", "_pizza", sizeof(my_pizza));

	int tickNum = 0;
	do {
		printf("%i,",tickNum);
		_pizza_syncOutputEvents(&my_pizza);
		_pizza_syncInputEvents(&my_pizza);

		_pizza_syncOutputData(&my_pizza);
		_pizza_syncInputData(&my_pizza);
		
		_pizza_run(&my_pizza);
		printf("\n");
	} while(tickNum++ < 200);

	return 0;
}


// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for _WaterHeaterPlantAndController
#include "FB__WaterHeaterPlantAndController.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* _WaterHeaterPlantAndController_preinit() is required to be called to 
 * initialise an instance of _WaterHeaterPlantAndController. 
 * It sets all I/O values to zero.
 */
int _WaterHeaterPlantAndController_preinit(_WaterHeaterPlantAndController_t  *me) {
	//if there are input events, reset them
	
	//if there are output events, reset them
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(WaterHeaterPlantODE_preinit(&me->p1) != 0) {
		return 1;
	}
	if(WaterHeaterPlantODE_preinit(&me->p2) != 0) {
		return 1;
	}
	if(Controller_preinit(&me->c) != 0) {
		return 1;
	}
	if(StartGen_preinit(&me->gen) != 0) {
		return 1;
	}
	
	
	//if this is a BFB/odeFB, set start state so that the start state is properly executed and _trigger if necessary
	
	
	return 0;
}

/* _WaterHeaterPlantAndController_init() is required to be called to 
 * set up an instance of _WaterHeaterPlantAndController. 
 * It passes around configuration data.
 */
int _WaterHeaterPlantAndController_init(_WaterHeaterPlantAndController_t  *me) {
	//pass in any parameters on this level
	me->p1.K = 0.075;
	me->p1.H = 150;
	me->p2.K = 0.075;
	me->p2.H = 150;
	me->gen.SetDeltaTime = 0.5;
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	me->c.T1 = me->p1.Y;
	me->c.T2 = me->p2.Y;
	me->p1.Heat = me->c.Heat1;
	me->p2.Heat = me->c.Heat2;
	me->p1.DeltaTime = me->gen.DeltaTime;
	me->p2.DeltaTime = me->gen.DeltaTime;
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(WaterHeaterPlantODE_init(&me->p1) != 0) {
		return 1;
	}
	if(WaterHeaterPlantODE_init(&me->p2) != 0) {
		return 1;
	}
	if(Controller_init(&me->c) != 0) {
		return 1;
	}
	if(StartGen_init(&me->gen) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* _WaterHeaterPlantAndController_syncOutputEvents() synchronises the output events of an
 * instance of _WaterHeaterPlantAndController as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _WaterHeaterPlantAndController_syncOutputEvents(_WaterHeaterPlantAndController_t  *me) {
	//first, for all cfb children, call this same function
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
}

/* _WaterHeaterPlantAndController_syncInputEvents() synchronises the input events of an
 * instance of _WaterHeaterPlantAndController as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _WaterHeaterPlantAndController_syncInputEvents(_WaterHeaterPlantAndController_t  *me) {
	//first, we explicitly synchronise the children
	
	me->p1.inputEvents.event.HeatChange = me->c.outputEvents.event.HeatChange; 
	
	me->p1.inputEvents.event.Start = me->gen.outputEvents.event.Start; 
	
	me->p2.inputEvents.event.HeatChange = me->c.outputEvents.event.HeatChange; 
	
	me->p2.inputEvents.event.Start = me->gen.outputEvents.event.Start; 
	
	me->c.inputEvents.event.TChange1 = me->p1.outputEvents.event.Ychange; 
	
	me->c.inputEvents.event.TChange2 = me->p2.outputEvents.event.Ychange; 
	

	//then, call this same function on all cfb children
	
}

/* _WaterHeaterPlantAndController_syncOutputData() synchronises the output data connections of an
 * instance of _WaterHeaterPlantAndController as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _WaterHeaterPlantAndController_syncOutputData(_WaterHeaterPlantAndController_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	
	
}

/* _WaterHeaterPlantAndController_syncInputData() synchronises the input data connections of an
 * instance of _WaterHeaterPlantAndController as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _WaterHeaterPlantAndController_syncInputData(_WaterHeaterPlantAndController_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for p1 (of type WaterHeaterPlantODE) which is a BFB
	
	if(me->p1.inputEvents.event.HeatChange == 1) { 
		me->p1.Heat = me->c.Heat1;
	} 
	if(me->p1.inputEvents.event.Start == 1) { 
		me->p1.DeltaTime = me->gen.DeltaTime;
	} 
	
	//sync for p2 (of type WaterHeaterPlantODE) which is a BFB
	
	if(me->p2.inputEvents.event.HeatChange == 1) { 
		me->p2.Heat = me->c.Heat2;
	} 
	if(me->p2.inputEvents.event.Start == 1) { 
		me->p2.DeltaTime = me->gen.DeltaTime;
	} 
	
	//sync for c (of type Controller) which is a BFB
	
	if(me->c.inputEvents.event.TChange1 == 1) { 
		me->c.T1 = me->p1.Y;
	} 
	if(me->c.inputEvents.event.TChange2 == 1) { 
		me->c.T2 = me->p2.Y;
	} 
	
	//sync for gen (of type StartGen) which is a BFB
	
	
	
	//for all composite function block children, call this same function
	
	
}


/* _WaterHeaterPlantAndController_run() executes a single tick of an
 * instance of _WaterHeaterPlantAndController according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void _WaterHeaterPlantAndController_run(_WaterHeaterPlantAndController_t  *me) {
	WaterHeaterPlantODE_run(&me->p1);
	WaterHeaterPlantODE_run(&me->p2);
	Controller_run(&me->c);
	StartGen_run(&me->gen);
	
}


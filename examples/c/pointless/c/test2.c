// This file has been automatically generated by go-iec61499 and should not be edited by hand
// Converter written by Hammond Pearce and available at github.com/kiwih/go-iec61499

// This file represents the implementation of the Composite Function Block for test2
#include "test2.h"

//When running a composite block, note that you would call the functions in this order
//_init(); 
//do {
//_syncEvents();
//_syncData();
//_run();
//} loop;


/* test2_init() is required to be called to 
 * initialise an instance of test2. 
 * It sets all I/O values to zero.
 */
void test2_init(struct test2 *me) {
	//if there are input events, reset them
	
	//if there are output events, reset them
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	me->ac1_default_var[0] = 0;
	me->ac1_default_var[1] = 1;
	me->ac1_default_var[2] = 2;
	me->ac1_default_var[3] = 3;
	me->ac2_default_var[0] = 3;
	me->ac2_default_var[1] = 2;
	me->ac2_default_var[2] = 1;
	me->ac2_default_var[3] = 0;
	
	//if there are fb children (CFBs only), call this same function on them
	ArrayCopier_init(&me->ac1);
	ArrayCopier_init(&me->ac2);
	
	//if this is a BFB, set _trigger to be true and start state so that the start state is properly executed
	
}



/* test2_syncEvents() synchronises the events of an
 * instance of test2 as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void test2_syncEvents(struct test2 *me) {
	//for all composite function block children, call this same function
	
	//for all basic function block children, perform their synchronisations explicitly
	//events are always copied
	me->ac2.inputEvents.event.in = me->ac1.outputEvents.event.out;
	me->ac1.inputEvents.event.set_default_in = me->ac1.outputEvents.event.set_default_out;
	me->ac1.inputEvents.event.in = me->ac2.outputEvents.event.out;
	me->ac2.inputEvents.event.set_default_in = me->ac2.outputEvents.event.set_default_out;
	
}

/* test2_syncData() synchronises the data connections of an
 * instance of test2 as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void test2_syncData(struct test2 *me) {
	//for all composite function block children, call this same function
	
	//for all basic function block children, perform their synchronisations explicitly
	//Data is sometimes copied
	
	//sync for ac1 (of type ArrayCopier) which is a BFB
	
	if(me->ac1.inputEvents.event.in == 1) { 
		
		me->ac1.in_var[0] = me->ac2.out_var[0];
		me->ac1.in_var[1] = me->ac2.out_var[1];
		me->ac1.in_var[2] = me->ac2.out_var[2];
		me->ac1.in_var[3] = me->ac2.out_var[3];
		
	} 
	if(me->ac1.inputEvents.event.set_default_in == 1) { 
		
		me->ac1.default_var[0] = me->ac1_default_var[0];
		me->ac1.default_var[1] = me->ac1_default_var[1];
		me->ac1.default_var[2] = me->ac1_default_var[2];
		me->ac1.default_var[3] = me->ac1_default_var[3];
		
	} 
	
	//sync for ac2 (of type ArrayCopier) which is a BFB
	
	if(me->ac2.inputEvents.event.in == 1) { 
		
		me->ac2.in_var[0] = me->ac1.out_var[0];
		me->ac2.in_var[1] = me->ac1.out_var[1];
		me->ac2.in_var[2] = me->ac1.out_var[2];
		me->ac2.in_var[3] = me->ac1.out_var[3];
		
	} 
	if(me->ac2.inputEvents.event.set_default_in == 1) { 
		
		me->ac2.default_var[0] = me->ac2_default_var[0];
		me->ac2.default_var[1] = me->ac2_default_var[1];
		me->ac2.default_var[2] = me->ac2_default_var[2];
		me->ac2.default_var[3] = me->ac2_default_var[3];
		
	} 
	

}


/* test2_run() executes a single tick of an
 * instance of test2 according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void test2_run(struct test2 *me) {
	ArrayCopier_run(&me->ac1);
	ArrayCopier_run(&me->ac2);
	
}


// This file has been automatically generated by go-iec61499 and should not be edited by hand
// Converter written by Hammond Pearce and available at github.com/kiwih/go-iec61499

// This file represents the implementation of the Composite Function Block for ResourceType1
#include "ResourceType1.h"

//When running a composite block, note that you would call the functions in this order
//_init(); 
//do {
//_syncEvents();
//_syncData();
//_run();
//} loop;

/* ResourceType1_init() is required to be called to 
 * initialise an instance of ResourceType1. 
 * Notice that this also calls the _init functions of any FB child instances.
 */
void ResourceType1_init(struct ResourceType1 *me) {
	CounterBFB_init(&me->counter);
	

	//if there are input events, reset them
	
	//if there are output events, reset them
	
	//if there are input vars, reset them
	
	//if there are output vars, reset them
	
	//CFBs have no internal vars

	//if there is any resource vars, reset them
	
	me->Variable1 = 0;
	
}

/* ResourceType1_syncEvents() synchronises the events of an
 * instance of ResourceType1 as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void ResourceType1_syncEvents(struct ResourceType1 *me) {
	//for all composite function block children, call this same function
	
	//for all basic function block children, perform their synchronisations explicitly
	//events are always copied
	
}

/* ResourceType1_syncData() synchronises the data connections of an
 * instance of ResourceType1 as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void ResourceType1_syncData(struct ResourceType1 *me) {
	//for all composite function block children, call this same function
	
	//for all basic function block children, perform their synchronisations explicitly
	//Data is sometimes copied
	
	//sync for counter (of type CounterBFB) which is a BFB
	
	

}


/* ResourceType1_run() executes a single tick of an
 * instance of ResourceType1 according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void ResourceType1_run(struct ResourceType1 *me) {
	CounterBFB_run(&me->counter);
	
}


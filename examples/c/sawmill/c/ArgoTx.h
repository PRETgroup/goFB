// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block ArgoTx
#ifndef ARGOTX_H_
#define ARGOTX_H_

#include "fbtypes.h"
#include "libmp/mp.h"

//This is a BFB, so we need an enum type for the state machine
enum ArgoTx_states { STATE_ArgoTx_Start };


union ArgoTxInputEvents {
	struct {
		UDINT DataPresent : 1;
	} event;
	UDINT events[1];
};


union ArgoTxOutputEvents {
	struct {
		UDINT SuccessChanged : 1;
	} event;
	UDINT events[1];
};


struct ArgoTx {
    //input events
	union ArgoTxInputEvents inputEvents;

    //output events
	union ArgoTxOutputEvents outputEvents;

    //input vars
	INT Data;
    UDINT ChanId;
    
    //output vars
	BOOL Success;
    
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	enum ArgoTx_states _state; //stores current state
	BOOL _trigger; //indicates if a state transition has occured this tick

	qpd_t* chan;
	volatile INT _SPM* write_data;
};

//all FBs get a preinit function
int ArgoTx_preinit(struct ArgoTx *me);

//all FBs get an init function
int ArgoTx_init(struct ArgoTx *me);

//all FBs get a run function
void ArgoTx_run(struct ArgoTx *me);




#endif

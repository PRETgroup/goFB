// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for Netw
#include "FB_Netw.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* Netw_preinit() is required to be called to 
 * initialise an instance of Netw. 
 * It sets all I/O values to zero.
 */
int Netw_preinit(Netw_t  *me) {
	//if there are input events, reset them
	
	//if there are output events, reset them
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Incr_preinit(&me->incrr) != 0) {
		return 1;
	}
	if(Emitr_preinit(&me->emitrr) != 0) {
		return 1;
	}
	if(Printr_preinit(&me->printrr) != 0) {
		return 1;
	}
	
	
	//if this is a BFB/odeFB, set start state so that the start state is properly executed and _trigger if necessary
	

	return 0;
}

/* Netw_init() is required to be called to 
 * set up an instance of Netw. 
 * It passes around configuration data.
 */
int Netw_init(Netw_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	me->printrr.pDat = me->incrr.txdat;
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Incr_init(&me->incrr) != 0) {
		return 1;
	}
	if(Emitr_init(&me->emitrr) != 0) {
		return 1;
	}
	if(Printr_init(&me->printrr) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* Netw_syncOutputEvents() synchronises the output events of an
 * instance of Netw as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Netw_syncOutputEvents(Netw_t  *me) {
	//first, for all cfb children, call this same function
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
}

/* Netw_syncInputEvents() synchronises the input events of an
 * instance of Netw as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Netw_syncInputEvents(Netw_t  *me) {
	//first, we explicitly synchronise the children
	
	me->incrr.inputEvents.event.rx = me->emitrr.outputEvents.event.e; 
	
	me->printrr.inputEvents.event.p = me->incrr.outputEvents.event.tx; 
	

	//then, call this same function on all cfb children
	
}

/* Netw_syncOutputData() synchronises the output data connections of an
 * instance of Netw as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Netw_syncOutputData(Netw_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	
	
}

/* Netw_syncInputData() synchronises the input data connections of an
 * instance of Netw as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Netw_syncInputData(Netw_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for incrr (of type Incr) which is a BFB
	
	
	//sync for emitrr (of type Emitr) which is a BFB
	
	
	//sync for printrr (of type Printr) which is a BFB
	
	if(me->printrr.inputEvents.event.p == 1) { 
		me->printrr.pDat = me->incrr.txdat;
	} 
	
	
	//for all composite function block children, call this same function
	
	
}


/* Netw_run() executes a single tick of an
 * instance of Netw according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void Netw_run(Netw_t  *me) {
	Incr_run(&me->incrr);
	Emitr_run(&me->emitrr);
	Printr_run(&me->printrr);
	
}


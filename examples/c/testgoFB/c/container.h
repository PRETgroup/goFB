// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block container
#ifndef CONTAINER_H_
#define CONTAINER_H_

#include "fbtypes.h"

//This is a CFB, so we need the #includes for the child blocks embedded here
#include "passforward.h"


union containerInputEvents {
	struct {
		UDINT DataInChanged : 1;
	} event;
	UDINT events[1];
};


union containerOutputEvents {
	struct {
		UDINT DataOutChanged : 1;
	} event;
	UDINT events[1];
};


typedef struct {
    //input events
	union containerInputEvents inputEvents;

    //output events
	union containerOutputEvents outputEvents;

    //input vars
	BOOL DataIn;
    
    //output vars
	BOOL DataOut;
    
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	passforward_t inside;
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	
} container_t;

//all FBs get a preinit function
int container_preinit(container_t  *me);

//all FBs get an init function
int container_init(container_t  *me);

//all FBs get a run function
void container_run(container_t  *me);

//composite/resource/device FBs get sync functions
void container_syncOutputEvents(container_t  *me);
void container_syncInputEvents(container_t  *me);

void container_syncOutputData(container_t  *me);
void container_syncInputData(container_t  *me);



#endif

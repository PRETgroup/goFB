// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block container_one
#ifndef CONTAINER_ONE_H_
#define CONTAINER_ONE_H_

#include "fbtypes.h"

//This is a CFB, so we need the #includes for the child blocks embedded here
#include "passforward.h"


union container_oneInputEvents {
	struct {
		UDINT DataInChanged;
	} event;
	
};


union container_oneOutputEvents {
	struct {
		UDINT DataOutChanged;
	} event;
	
};


typedef struct {
    //input events
	union container_oneInputEvents inputEvents;

    //output events
	union container_oneOutputEvents outputEvents;

    //input vars
	INT DataIn;
    INT printf_id;
    
    //output vars
	INT DataOut;
    
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	passforward_t inside;
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	
} container_one_t;

//all FBs get a preinit function
int container_one_preinit(container_one_t  *me);

//all FBs get an init function
int container_one_init(container_one_t  *me);

//all FBs get a run function
void container_one_run(container_one_t  *me);

//composite/resource/device FBs get sync functions
void container_one_syncOutputEvents(container_one_t  *me);
void container_one_syncInputEvents(container_one_t  *me);

void container_one_syncOutputData(container_one_t  *me);
void container_one_syncInputData(container_one_t  *me);



#endif

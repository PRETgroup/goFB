// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for _CoreSingle
#include "_CoreSingle.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* _CoreSingle_preinit() is required to be called to 
 * initialise an instance of _CoreSingle. 
 * It sets all I/O values to zero.
 */
int _CoreSingle_preinit(_CoreSingle_t  *me) {
	//if there are input events, reset them
	
	//if there are output events, reset them
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(SawmillModule_preinit(&me->saw1) != 0) {
		return 1;
	}
	if(SawmillModule_preinit(&me->saw2) != 0) {
		return 1;
	}
	if(SawmillModule_preinit(&me->saw3) != 0) {
		return 1;
	}
	if(PrintStatus_preinit(&me->statusprint) != 0) {
		return 1;
	}
	
	
	//if this is a BFB, set _trigger to be true and start state so that the start state is properly executed
	

	return 0;
}

/* _CoreSingle_init() is required to be called to 
 * set up an instance of _CoreSingle. 
 * It passes around configuration data.
 */
int _CoreSingle_init(_CoreSingle_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	me->statusprint.Saw1Status = me->saw1.Message;
	me->statusprint.Saw2Status = me->saw2.Message;
	me->statusprint.Saw3Status = me->saw3.Message;
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(SawmillModule_init(&me->saw1) != 0) {
		return 1;
	}
	if(SawmillModule_init(&me->saw2) != 0) {
		return 1;
	}
	if(SawmillModule_init(&me->saw3) != 0) {
		return 1;
	}
	if(PrintStatus_init(&me->statusprint) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* _CoreSingle_syncOutputEvents() synchronises the output events of an
 * instance of _CoreSingle as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _CoreSingle_syncOutputEvents(_CoreSingle_t  *me) {
	//first, for all cfb children, call this same function
	
	SawmillModule_syncOutputEvents(&me->saw1);//sync for saw1 (of type SawmillModule) which is a CFB
	
	SawmillModule_syncOutputEvents(&me->saw2);//sync for saw2 (of type SawmillModule) which is a CFB
	
	SawmillModule_syncOutputEvents(&me->saw3);//sync for saw3 (of type SawmillModule) which is a CFB
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
}

/* _CoreSingle_syncInputEvents() synchronises the input events of an
 * instance of _CoreSingle as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _CoreSingle_syncInputEvents(_CoreSingle_t  *me) {
	//first, we explicitly synchronise the children
	
	me->statusprint.inputEvents.event.StatusUpdate = me->saw1.outputEvents.event.MessageChange || me->saw2.outputEvents.event.MessageChange || me->saw3.outputEvents.event.MessageChange; 
	

	//then, call this same function on all cfb children
	
	SawmillModule_syncInputEvents(&me->saw1);//sync for saw1 (of type SawmillModule) which is a CFB
	
	SawmillModule_syncInputEvents(&me->saw2);//sync for saw2 (of type SawmillModule) which is a CFB
	
	SawmillModule_syncInputEvents(&me->saw3);//sync for saw3 (of type SawmillModule) which is a CFB
	
}

/* _CoreSingle_syncOutputData() synchronises the output data connections of an
 * instance of _CoreSingle as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _CoreSingle_syncOutputData(_CoreSingle_t  *me) {
	//for all composite function block children, call this same function
	//sync for saw1 (of type SawmillModule) which is a CFB
	SawmillModule_syncOutputData(&me->saw1);//sync for saw2 (of type SawmillModule) which is a CFB
	SawmillModule_syncOutputData(&me->saw2);//sync for saw3 (of type SawmillModule) which is a CFB
	SawmillModule_syncOutputData(&me->saw3);
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	
	
}

/* _CoreSingle_syncInputData() synchronises the input data connections of an
 * instance of _CoreSingle as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void _CoreSingle_syncInputData(_CoreSingle_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	//sync for saw1 (of Type SawmillModule) which is a CFB
	//sync for saw2 (of Type SawmillModule) which is a CFB
	//sync for saw3 (of Type SawmillModule) which is a CFB
	
	//sync for statusprint (of type PrintStatus) which is a BFB
	
	if(me->statusprint.inputEvents.event.StatusUpdate == 1) { 
		me->statusprint.Saw1Status = me->saw1.Message;
		me->statusprint.Saw2Status = me->saw2.Message;
		me->statusprint.Saw3Status = me->saw3.Message;
	} 
	
	
	//for all composite function block children, call this same function
	//sync for saw1 (of type SawmillModule) which is a CFB
	SawmillModule_syncInputData(&me->saw1);//sync for saw2 (of type SawmillModule) which is a CFB
	SawmillModule_syncInputData(&me->saw2);//sync for saw3 (of type SawmillModule) which is a CFB
	SawmillModule_syncInputData(&me->saw3);
	
}


/* _CoreSingle_run() executes a single tick of an
 * instance of _CoreSingle according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void _CoreSingle_run(_CoreSingle_t  *me) {
	SawmillModule_run(&me->saw1);
	SawmillModule_run(&me->saw2);
	SawmillModule_run(&me->saw3);
	PrintStatus_run(&me->statusprint);
	
}


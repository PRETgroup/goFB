// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block Gen
#ifndef GEN_H_
#define GEN_H_

#include "fbtypes.h"

//This is a BFB, so we need an enum type for the state machine
enum Gen_states { STATE_Gen_Start, STATE_Gen_count, STATE_Gen_wait };


//this block had no input events


union GenOutputEvents {
	struct {
		UDINT CountChanged;
	} event;
	
};


typedef struct {
    //input events
	

    //output events
	union GenOutputEvents outputEvents;

    //input vars
	
    //output vars
	LINT Count;
    
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	enum Gen_states _state; //stores current state
	BOOL _trigger; //indicates if a state transition has occured this tick
	
} Gen_t;

//all FBs get a preinit function
int Gen_preinit(Gen_t  *me);

//all FBs get an init function
int Gen_init(Gen_t  *me);

//all FBs get a run function
void Gen_run(Gen_t  *me);

//basic FBs have a number of algorithm functions

void Gen_IncCountService(Gen_t  *me);


#endif

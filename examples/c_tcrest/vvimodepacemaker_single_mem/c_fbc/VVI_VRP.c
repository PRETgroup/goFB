// This file is generated by FBC.

#include "VVI_VRP.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void VVI_VRPinit(VVI_VRP* me)
{
    me->_state = 0;
    me->_entered = false;
}

/* ECC algorithms */
void VVI_VRP_VRP_Set_Timeout_Value(VVI_VRP* me)
{
me -> VRP_Timeout_Value = 300;
}

/* Function block execution function */
void VVI_VRPrun(VVI_VRP* me)
{
    me->_output.event.VRP_Start_Timer = 0;
	me->_output.event.VSense = 0;
	me->_output.event.VRefractory = 0;

	#pragma loopbound min 1 max 2
    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: Detected_VPulse
                if (!me->_entered) {
                    VVI_VRP_VRP_Set_Timeout_Value(me);
                    me->_output.event.VRP_Start_Timer = 1;
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.VRP_Timer_Timeout) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: Wait_For_VSense
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.VPulse) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                    else if (me->_input.event.VPace) {
                        me->_state = 3;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 3:
                // State: Resting
                if (!me->_entered) {
                    VVI_VRP_VRP_Set_Timeout_Value(me);
                    me->_output.event.VRP_Start_Timer = 1;
                    me->_output.event.VSense = 1;
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.VPulse) {
                        me->_state = 4;
                        me->_entered = false;
                        continue;
                    }
                    else if (me->_input.event.VRP_Timer_Timeout) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 4:
                // State: Detected_VRefractory
                if (!me->_entered) {
                    me->_output.event.VRefractory = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 3;
                    me->_entered = false;
                    continue;
                }
                break;
        }
        break;
    }
    if (me->_output.event.VRP_Start_Timer) {
        me->_VRP_Timeout_Value = me->VRP_Timeout_Value;
    }
}


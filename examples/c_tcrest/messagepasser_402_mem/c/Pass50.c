// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for Pass50
#include "Pass50.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* Pass50_preinit() is required to be called to 
 * initialise an instance of Pass50. 
 * It sets all I/O values to zero.
 */
int Pass50_preinit(Pass50_t  *me) {
	//if there are input events, reset them
	me->inputEvents.event.CountChanged = 0;
	
	//if there are output events, reset them
	me->outputEvents.event.OutCountChanged = 0;
	
	//if there are input vars with default values, set them
	
	//if there are output vars with default values, set them
	
	//if there are internal vars with default values, set them (BFBs only)
	
	//if there are resource vars with default values, set them
	
	//if there are resources with set parameters, set them
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Pass10_preinit(&me->c1) != 0) {
		return 1;
	}
	if(Pass10_preinit(&me->c2) != 0) {
		return 1;
	}
	if(Pass10_preinit(&me->c3) != 0) {
		return 1;
	}
	if(Pass10_preinit(&me->c4) != 0) {
		return 1;
	}
	if(Pass10_preinit(&me->c5) != 0) {
		return 1;
	}
	
	
	//if this is a BFB, set _trigger to be true and start state so that the start state is properly executed
	

	return 0;
}

/* Pass50_init() is required to be called to 
 * set up an instance of Pass50. 
 * It passes around configuration data.
 */
int Pass50_init(Pass50_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	me->c1.Count = me->Count;
	me->OutCount = me->c5.OutCount;
	me->c2.Count = me->c1.OutCount;
	me->c3.Count = me->c2.OutCount;
	me->c4.Count = me->c3.OutCount;
	me->c5.Count = me->c4.OutCount;
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Pass10_init(&me->c1) != 0) {
		return 1;
	}
	if(Pass10_init(&me->c2) != 0) {
		return 1;
	}
	if(Pass10_init(&me->c3) != 0) {
		return 1;
	}
	if(Pass10_init(&me->c4) != 0) {
		return 1;
	}
	if(Pass10_init(&me->c5) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* Pass50_syncOutputEvents() synchronises the output events of an
 * instance of Pass50 as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Pass50_syncOutputEvents(Pass50_t  *me) {
	//first, for all cfb children, call this same function
	
	Pass10_syncOutputEvents(&me->c1);//sync for c1 (of type Pass10) which is a CFB
	
	Pass10_syncOutputEvents(&me->c2);//sync for c2 (of type Pass10) which is a CFB
	
	Pass10_syncOutputEvents(&me->c3);//sync for c3 (of type Pass10) which is a CFB
	
	Pass10_syncOutputEvents(&me->c4);//sync for c4 (of type Pass10) which is a CFB
	
	Pass10_syncOutputEvents(&me->c5);//sync for c5 (of type Pass10) which is a CFB
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
	me->outputEvents.event.OutCountChanged = me->c5.outputEvents.event.OutCountChanged; 
	
}

/* Pass50_syncInputEvents() synchronises the input events of an
 * instance of Pass50 as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Pass50_syncInputEvents(Pass50_t  *me) {
	//first, we explicitly synchronise the children
	
	me->c1.inputEvents.event.CountChanged = me->inputEvents.event.CountChanged; 
	
	me->c2.inputEvents.event.CountChanged = me->c1.outputEvents.event.OutCountChanged; 
	
	me->c3.inputEvents.event.CountChanged = me->c2.outputEvents.event.OutCountChanged; 
	
	me->c4.inputEvents.event.CountChanged = me->c3.outputEvents.event.OutCountChanged; 
	
	me->c5.inputEvents.event.CountChanged = me->c4.outputEvents.event.OutCountChanged; 
	

	//then, call this same function on all cfb children
	
	Pass10_syncInputEvents(&me->c1);//sync for c1 (of type Pass10) which is a CFB
	
	Pass10_syncInputEvents(&me->c2);//sync for c2 (of type Pass10) which is a CFB
	
	Pass10_syncInputEvents(&me->c3);//sync for c3 (of type Pass10) which is a CFB
	
	Pass10_syncInputEvents(&me->c4);//sync for c4 (of type Pass10) which is a CFB
	
	Pass10_syncInputEvents(&me->c5);//sync for c5 (of type Pass10) which is a CFB
	
}

/* Pass50_syncOutputData() synchronises the output data connections of an
 * instance of Pass50 as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Pass50_syncOutputData(Pass50_t  *me) {
	//for all composite function block children, call this same function
	//sync for c1 (of type Pass10) which is a CFB
	Pass10_syncOutputData(&me->c1);//sync for c2 (of type Pass10) which is a CFB
	Pass10_syncOutputData(&me->c2);//sync for c3 (of type Pass10) which is a CFB
	Pass10_syncOutputData(&me->c3);//sync for c4 (of type Pass10) which is a CFB
	Pass10_syncOutputData(&me->c4);//sync for c5 (of type Pass10) which is a CFB
	Pass10_syncOutputData(&me->c5);
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	me->OutCount = me->c5.OutCount;
	
	
}

/* Pass50_syncInputData() synchronises the input data connections of an
 * instance of Pass50 as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Pass50_syncInputData(Pass50_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	//sync for c1 (of Type Pass10) which is a CFB
	
	
		me->c1.Count = me->Count;
	//sync for c2 (of Type Pass10) which is a CFB
	
	
		me->c2.Count = me->c1.OutCount;
	//sync for c3 (of Type Pass10) which is a CFB
	
	
		me->c3.Count = me->c2.OutCount;
	//sync for c4 (of Type Pass10) which is a CFB
	
	
		me->c4.Count = me->c3.OutCount;
	//sync for c5 (of Type Pass10) which is a CFB
	
	
		me->c5.Count = me->c4.OutCount;
	
	
	//for all composite function block children, call this same function
	//sync for c1 (of type Pass10) which is a CFB
	Pass10_syncInputData(&me->c1);//sync for c2 (of type Pass10) which is a CFB
	Pass10_syncInputData(&me->c2);//sync for c3 (of type Pass10) which is a CFB
	Pass10_syncInputData(&me->c3);//sync for c4 (of type Pass10) which is a CFB
	Pass10_syncInputData(&me->c4);//sync for c5 (of type Pass10) which is a CFB
	Pass10_syncInputData(&me->c5);
	
}


/* Pass50_run() executes a single tick of an
 * instance of Pass50 according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void Pass50_run(Pass50_t  *me) {
	Pass10_run(&me->c1);
	Pass10_run(&me->c2);
	Pass10_run(&me->c3);
	Pass10_run(&me->c4);
	Pass10_run(&me->c5);
	
}


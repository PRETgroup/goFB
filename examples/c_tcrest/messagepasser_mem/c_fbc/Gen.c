// This file is generated by FBC.

#include "Gen.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void Geninit(Gen* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_output.event.CountChanged = 0;
    me->Count = 0;
    me->_Count = 0;
}

/* ECC algorithms */
void Gen_IncCountService(Gen* me)
{
me->Count++;
}

/* Function block execution function */
void Genrun(Gen* me)
{
    me->_output.event.CountChanged = 0;

	#pragma loopbound min 1 max 2
    for (;;) {
        switch (me->_state) {
            case 0:
                // State: Start
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: wait
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    me->_state = 2;
                    me->_entered = false;
                    continue;
                }
                break;
            case 2:
                // State: count
                if (!me->_entered) {
                    Gen_IncCountService(me);
                    me->_output.event.CountChanged = 1;
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
        }
        break;
    }
    if (me->_output.event.CountChanged) {
        me->_Count = me->Count;
    }

}


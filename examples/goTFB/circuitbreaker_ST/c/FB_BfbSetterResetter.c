// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Basic Function Block for BfbSetterResetter
#include "FB_BfbSetterResetter.h"


/* BfbSetterResetter_preinit() is required to be called to 
 * initialise an instance of BfbSetterResetter. 
 * It sets all I/O values to zero.
 */
int BfbSetterResetter_preinit(BfbSetterResetter_t  *me) {
	

	//reset the input events
	me->inputEvents.event.test = 0;
	//reset the input events
	me->inputEvents.event.set = 0;
	//reset the input events
	me->inputEvents.event.unsafe = 0;
	
	//reset the output events
	me->outputEvents.event.b_change = 0;
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	//set any internal vars with default values
	
	
	
	
	

	
	
	//if this is a BFB/odeFB, set start state so that the start state is properly executed and _trigger if necessary
	me->_state = STATE_BfbSetterResetter_s_init;
	me->_trigger = true;
	
	
	

	

	return 0;
}

/* BfbSetterResetter_init() is required to be called to 
 * set up an instance of BfbSetterResetter. 
 * It passes around configuration data.
 */
int BfbSetterResetter_init(BfbSetterResetter_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	
	

	return 0;
}



//algorithms

void BfbSetterResetter_s_reset_alg0(BfbSetterResetter_t  *me) {
//PROVIDED CODE: this algorithm was provided in an algorithm's text field
me->b = 0;

}

void BfbSetterResetter_s_set_alg0(BfbSetterResetter_t  *me) {
//PROVIDED CODE: this algorithm was provided in an algorithm's text field
me->b = 1;

}



/* BfbSetterResetter_run() executes a single tick of an
 * instance of BfbSetterResetter according to synchronous semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * will need to be done in the parent.
 * Also note that on the first run of this function, trigger will already be set
 * to true, meaning that on the very first run no next state logic will occur.
 */
void BfbSetterResetter_run(BfbSetterResetter_t  *me) {
	//if there are output events, reset them
	
	me->outputEvents.event.b_change = 0;
	
	
	

	
	//next state logic
	if(me->_trigger == false) {
		switch(me->_state) {
		case STATE_BfbSetterResetter_s_init:
			if(true) {
				
				me->_state = STATE_BfbSetterResetter_s_reset;
				me->_trigger = true;
			};
			break;
		case STATE_BfbSetterResetter_s_reset:
			if(me->inputEvents.event.set) {
				
				me->_state = STATE_BfbSetterResetter_s_set;
				me->_trigger = true;
			};
			break;
		case STATE_BfbSetterResetter_s_set:
			if(me->inputEvents.event.test || me->inputEvents.event.unsafe) {
				
				me->_state = STATE_BfbSetterResetter_s_reset;
				me->_trigger = true;
			};
			break;
		
		default: 
			break;
		}
	}

	//state output logic
	if(me->_trigger == true) {
		switch(me->_state) {
		case STATE_BfbSetterResetter_s_init:
			#ifdef PRINT_STATES
				printf("BfbSetterResetter: [Entered State s_init]\n");
			#endif
			
			break;
		case STATE_BfbSetterResetter_s_reset:
			#ifdef PRINT_STATES
				printf("BfbSetterResetter: [Entered State s_reset]\n");
			#endif
			me->outputEvents.event.b_change = 1;
			BfbSetterResetter_s_reset_alg0(me);
			
			break;
		case STATE_BfbSetterResetter_s_set:
			#ifdef PRINT_STATES
				printf("BfbSetterResetter: [Entered State s_set]\n");
			#endif
			me->outputEvents.event.b_change = 1;
			BfbSetterResetter_s_set_alg0(me);
			
			break;
		
		default: 
			break;
		}
	}

	me->_trigger = false;

	

	//Ensure input events are cleared
	me->inputEvents.event.test = 0;
	me->inputEvents.event.set = 0;
	me->inputEvents.event.unsafe = 0;
	
}



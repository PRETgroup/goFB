// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for CfbBreakerController
#include "FB_CfbBreakerController.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* CfbBreakerController_preinit() is required to be called to 
 * initialise an instance of CfbBreakerController. 
 * It sets all I/O values to zero.
 */
int CfbBreakerController_preinit(CfbBreakerController_t  *me) {
	

	//reset the input events
	me->inputEvents.event.tick = 0;
	//reset the input events
	me->inputEvents.event.i_measured = 0;
	//reset the input events
	me->inputEvents.event.test = 0;
	//reset the input events
	me->inputEvents.event.set = 0;
	//reset the input events
	me->inputEvents.event.iSet_change = 0;
	
	//reset the output events
	me->outputEvents.event.b_change = 0;
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	
	
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(BfbSetterResetter_preinit(&me->sr) != 0) {
		return 1;
	}
	
	if(BfbIDMTCurve_preinit(&me->curve) != 0) {
		return 1;
	}
	
	
	

	
	
	

	

	return 0;
}

/* CfbBreakerController_init() is required to be called to 
 * set up an instance of CfbBreakerController. 
 * It passes around configuration data.
 */
int CfbBreakerController_init(CfbBreakerController_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	//sync config for sr (of Type BfbSetterResetter) 
			//sync config for curve (of Type BfbIDMTCurve) 
			me->curve.i = me->i;
							me->curve.iSet = me->iSet;
							

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(BfbSetterResetter_init(&me->sr) != 0) {
		return 1;
	}
	if(BfbIDMTCurve_init(&me->curve) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* CfbBreakerController_syncOutputEvents() synchronises the output events of an
 * instance of CfbBreakerController as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbBreakerController_syncOutputEvents(CfbBreakerController_t  *me) {
	//first, for all cfb children, call this same function
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
	me->outputEvents.event.b_change = me->sr.outputEvents.event.b_change; 
		}

/* CfbBreakerController_syncInputEvents() synchronises the input events of an
 * instance of CfbBreakerController as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbBreakerController_syncInputEvents(CfbBreakerController_t  *me) {
	//first, we explicitly synchronise the children
	
	me->sr.inputEvents.event.test = me->inputEvents.event.test; 
	
	me->sr.inputEvents.event.set = me->inputEvents.event.set; 
	
	me->sr.inputEvents.event.unsafe = me->curve.outputEvents.event.unsafe; 
	
	me->curve.inputEvents.event.tick = me->inputEvents.event.tick; 
	
	me->curve.inputEvents.event.i_measured = me->inputEvents.event.i_measured; 
	
	me->curve.inputEvents.event.iSet_change = me->inputEvents.event.iSet_change; 
	

	//then, call this same function on all cfb children
	
}

/* CfbBreakerController_syncOutputData() synchronises the output data connections of an
 * instance of CfbBreakerController as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbBreakerController_syncOutputData(CfbBreakerController_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	me->b = me->sr.b;
	
	
}

/* CfbBreakerController_syncInputData() synchronises the input data connections of an
 * instance of CfbBreakerController as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbBreakerController_syncInputData(CfbBreakerController_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for sr (of type BfbSetterResetter) which is a BFB
	
	
	//sync for curve (of type BfbIDMTCurve) which is a BFB
	
	if(me->curve.inputEvents.event.i_measured == 1) {
		me->curve.i = me->i;
		
	} 
	if(me->curve.inputEvents.event.iSet_change == 1) {
		me->curve.iSet = me->iSet;
		
	} 
	
	
	//for all composite function block children, call this same function
	
	
}


/* CfbBreakerController_run() executes a single tick of an
 * instance of CfbBreakerController according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void CfbBreakerController_run(CfbBreakerController_t  *me) {
	
	
	BfbSetterResetter_run(&me->sr);
	
	BfbIDMTCurve_run(&me->curve);
	
}






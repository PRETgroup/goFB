// This file is generated by FBC.

#include "BfbSetterResetter.h"
#include <string.h>
#include <stdio.h>

/* Function block initialization function */
void BfbSetterResetterinit(BfbSetterResetter* me)
{
    me->_state = 0;
    me->_entered = false;
    me->_input.events = 0;
    me->_output.events = 0;
}

/* ECC algorithms */
void BfbSetterResetter_s_reset_alg0(BfbSetterResetter* me)
{
me->b=0;

}

void BfbSetterResetter_s_set_alg0(BfbSetterResetter* me)
{
me->b=1;

}

/* Function block execution function */
void BfbSetterResetterrun(BfbSetterResetter* me)
{
    me->_output.events = 0;

    for (;;) {
        switch (me->_state) {
            case 0:
                // State: s_init
                if (!me->_entered) {
                    me->_entered = true;
                }
                else {
                    me->_state = 1;
                    me->_entered = false;
                    continue;
                }
                break;
            case 1:
                // State: s_reset
                if (!me->_entered) {
                    me->_output.event.b_change = 1;
                    BfbSetterResetter_s_reset_alg0(me);
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.set) {
                        me->_state = 2;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
            case 2:
                // State: s_set
                if (!me->_entered) {
                    me->_output.event.b_change = 1;
                    BfbSetterResetter_s_set_alg0(me);
                    me->_entered = true;
                }
                else {
                    if (me->_input.event.test me->_input.event.unsafe) {
                        me->_state = 1;
                        me->_entered = false;
                        continue;
                    }
                }
                break;
        }
        break;
    }
    if (me->_output.event.b_change) {
        me->_b = me->b;
    }

    me->_input.events = 0;
}


// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for CfbTop
#include "FB_CfbTop.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* CfbTop_preinit() is required to be called to 
 * initialise an instance of CfbTop. 
 * It sets all I/O values to zero.
 */
int CfbTop_preinit(CfbTop_t  *me) {
	

	
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	
	
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(BfbCheck_preinit(&me->check) != 0) {
		return 1;
	}
	
	if(BfbGen_preinit(&me->gen) != 0) {
		return 1;
	}
	
	
	

	
	
	

	return 0;
}

/* CfbTop_init() is required to be called to 
 * set up an instance of CfbTop. 
 * It passes around configuration data.
 */
int CfbTop_init(CfbTop_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	//sync config for check (of Type BfbCheck) 
	
	
		me->check.genDat = me->gen.genDat;
	//sync config for gen (of Type BfbGen) 
	

	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(BfbCheck_init(&me->check) != 0) {
		return 1;
	}
	if(BfbGen_init(&me->gen) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* CfbTop_syncOutputEvents() synchronises the output events of an
 * instance of CfbTop as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbTop_syncOutputEvents(CfbTop_t  *me) {
	//first, for all cfb children, call this same function
	
	
	//then, for all connections that are connected to an output on the parent, run their run their copy
	
}

/* CfbTop_syncInputEvents() synchronises the input events of an
 * instance of CfbTop as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbTop_syncInputEvents(CfbTop_t  *me) {
	//first, we explicitly synchronise the children
	
	me->check.inputEvents.event.genE = me->gen.outputEvents.event.genE; 
	

	//then, call this same function on all cfb children
	
}

/* CfbTop_syncOutputData() synchronises the output data connections of an
 * instance of CfbTop as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbTop_syncOutputData(CfbTop_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	
	
}

/* CfbTop_syncInputData() synchronises the input data connections of an
 * instance of CfbTop as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void CfbTop_syncInputData(CfbTop_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for check (of type BfbCheck) which is a BFB
	
	if(me->check.inputEvents.event.genE == 1) { me->check.genDat = me->gen.genDat;
		
	} 
	
	//sync for gen (of type BfbGen) which is a BFB
	
	
	
	//for all composite function block children, call this same function
	
	
}


/* CfbTop_run() executes a single tick of an
 * instance of CfbTop according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void CfbTop_run(CfbTop_t  *me) {
	
	
	BfbCheck_run(&me->check);
	
	BfbGen_run(&me->gen);
	
}






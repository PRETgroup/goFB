// This file has been automatically generated by goFB
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

//This is the main file for the iec61499 network with CfbTop as the top level block

#include "FB_CfbTop.h"
#include <sys/time.h>

// #ifndef MAX_TICKS
// #define MAX_TICKS 100
// #endif



//put a copy of the top level block into global memory
CfbTop_t CfbTop;

int main() {
	if(CfbTop_preinit(&CfbTop) != 0) {
		printf("Failed to preinitialize.");
		return 1;
	}
	if(CfbTop_init(&CfbTop) != 0) {
		printf("Failed to initialize.");
		return 1;
	}
	printf("\n");
	
//this is executing with synchronous semantics
	//printf("\n\n\n");
	//printf("\nTop: %20s   Size: %lu\n", "CfbTop", sizeof(myCfbTop));

	#ifdef PRINT_VALS
	printf("Simulation time,");
	#endif
	
	struct timeval tv1, tv2;
	gettimeofday(&tv1, NULL);

	int tickNum = 1;
	do {
		#ifdef PRINT_VALS
			printf("%f,",(double)tickNum*0.01);
		#endif
		CfbTop_syncOutputEvents(&CfbTop);
		CfbTop_syncInputEvents(&CfbTop);

		CfbTop_syncOutputData(&CfbTop);
		CfbTop_syncInputData(&CfbTop);
		
		CfbTop_run(&CfbTop);
		#ifdef PRINT_VALS
			printf("\n");
		#endif
	} 
	#ifdef MAX_TICKS
		while(tickNum++ < MAX_TICKS);
	#else
		while(1);
	#endif
	gettimeofday(&tv2, NULL);
	#ifdef PRINT_TIME
	printf ("Total time = %f seconds\n",
         (double) (tv2.tv_usec - tv1.tv_usec) / 1000000 +
         (double) (tv2.tv_sec - tv1.tv_sec));
	#endif
	return 0;

}




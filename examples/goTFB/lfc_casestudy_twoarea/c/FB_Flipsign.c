// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Basic Function Block for Flipsign
#include "FB_Flipsign.h"


/* Flipsign_preinit() is required to be called to 
 * initialise an instance of Flipsign. 
 * It sets all I/O values to zero.
 */
int Flipsign_preinit(Flipsign_t  *me) {
	

	//reset the input events
	me->inputEvents.event.InChange = 0;
	
	//reset the output events
	me->outputEvents.event.OutChange = 0;
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	//set any internal vars with default values
	
	
	
	
	

	
	
	//if this is a BFB/odeFB, set start state so that the start state is properly executed and _trigger if necessary
	me->_state = STATE_Flipsign_reset;
	me->_trigger = true;
	
	
	

	

	return 0;
}

/* Flipsign_init() is required to be called to 
 * set up an instance of Flipsign. 
 * It passes around configuration data.
 */
int Flipsign_init(Flipsign_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	
	

	return 0;
}



//algorithms

void Flipsign_update_alg0(Flipsign_t  *me) {
//PROVIDED CODE: this algorithm was provided in an algorithm's text field
me->Out = -1 * me->In;

}



/* Flipsign_run() executes a single tick of an
 * instance of Flipsign according to synchronous semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * will need to be done in the parent.
 * Also note that on the first run of this function, trigger will already be set
 * to true, meaning that on the very first run no next state logic will occur.
 */
void Flipsign_run(Flipsign_t  *me) {
	//if there are output events, reset them
	
	me->outputEvents.event.OutChange = 0;
	
	
	

	
	//next state logic
	if(me->_trigger == false) {
		switch(me->_state) {
		case STATE_Flipsign_reset:
			if(me->inputEvents.event.InChange) {
				
				me->_state = STATE_Flipsign_update;
				me->_trigger = true;
			};
			break;
		case STATE_Flipsign_update:
			if(me->inputEvents.event.InChange) {
				
				me->_state = STATE_Flipsign_update;
				me->_trigger = true;
			};
			break;
		
		default: 
			break;
		}
	}

	//state output logic
	if(me->_trigger == true) {
		switch(me->_state) {
		case STATE_Flipsign_reset:
			#ifdef PRINT_STATES
				printf("Flipsign: [Entered State reset]\n");
			#endif
			
			break;
		case STATE_Flipsign_update:
			#ifdef PRINT_STATES
				printf("Flipsign: [Entered State update]\n");
			#endif
			me->outputEvents.event.OutChange = 1;
			Flipsign_update_alg0(me);
			
			break;
		
		default: 
			break;
		}
	}

	me->_trigger = false;

	

	//Ensure input events are cleared
	me->inputEvents.event.InChange = 0;
	
}



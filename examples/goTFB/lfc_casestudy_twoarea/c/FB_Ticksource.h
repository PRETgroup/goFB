// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the interface of Function Block Ticksource
#ifndef TICKSOURCE_H_
#define TICKSOURCE_H_

#include "fbtypes.h"
#include "util.h"



//This is a BFB with states, so we need an enum type for the state machine
enum Ticksource_states { STATE_Ticksource_reset, STATE_Ticksource_print, STATE_Ticksource_tie, STATE_Ticksource_ic, STATE_Ticksource_load, STATE_Ticksource_gen, STATE_Ticksource_update, STATE_Ticksource_update_load };




//this block had no input events


union TicksourceOutputEvents {
	struct {
		UDINT TickIc;
		UDINT TickGen;
		UDINT TickLoad;
		UDINT TickTie;
		UDINT TickPrint;
	} event;
	
};


typedef struct {
    //input events
	

    //output events
	union TicksourceOutputEvents outputEvents;

    //input vars
	
    //output vars
	
	//any internal vars (BFBs only)
    
	//any child FBs (CFBs only)
	
	//resource vars
	
	//resources (Devices only)
	
	//state and trigger (BFBs only)
	enum Ticksource_states _state; //stores current state
	BOOL _trigger; //indicates if a state transition has occured this tick
	
	
	

	

} Ticksource_t;

//all FBs get a preinit function
int Ticksource_preinit(Ticksource_t  *me);

//all FBs get an init function
int Ticksource_init(Ticksource_t  *me);

//all FBs get a run function
void Ticksource_run(Ticksource_t  *me);



#endif

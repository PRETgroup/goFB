// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for Top
#include "FB_Top.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* Top_preinit() is required to be called to 
 * initialise an instance of Top. 
 * It sets all I/O values to zero.
 */
int Top_preinit(Top_t  *me) {
	

	
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	
	
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Ticksource_preinit(&me->ticksource) != 0) {
		return 1;
	}
	
	if(Generator_preinit(&me->gen1) != 0) {
		return 1;
	}
	
	if(Generator_preinit(&me->gen2) != 0) {
		return 1;
	}
	
	if(IntegralController_preinit(&me->ic1) != 0) {
		return 1;
	}
	
	if(IntegralController_preinit(&me->ic2) != 0) {
		return 1;
	}
	
	if(Load_preinit(&me->load1) != 0) {
		return 1;
	}
	
	if(Load_preinit(&me->load2) != 0) {
		return 1;
	}
	
	if(Flipsign_preinit(&me->flipsignDp12) != 0) {
		return 1;
	}
	
	if(Tieline_preinit(&me->tieline) != 0) {
		return 1;
	}
	
	if(LfcPrint_preinit(&me->print) != 0) {
		return 1;
	}
	
	
	

	
	
	

	

	return 0;
}

/* Top_init() is required to be called to 
 * set up an instance of Top. 
 * It passes around configuration data.
 */
int Top_init(Top_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	//sync config for ticksource (of Type Ticksource) 
			//sync config for gen1 (of Type Generator) 
			me->gen1.Dpref = me->ic1.Dpref;
							me->gen1.Dpe = me->load1.Dpe;
							//sync config for gen2 (of Type Generator) 
			me->gen2.Dpref = me->ic2.Dpref;
							me->gen2.Dpe = me->load2.Dpe;
							//sync config for ic1 (of Type IntegralController) 
			me->ic1.Df = me->gen1.Df;
							me->ic1.Dp12 = me->tieline.Dp12;
							//sync config for ic2 (of Type IntegralController) 
			me->ic2.Df = me->gen2.Df;
							me->ic2.Dp12 = me->flipsignDp12.Out;
							//sync config for load1 (of Type Load) 
			me->load1.Dp12 = me->tieline.Dp12;
							me->load1.DpeExternal = 0;
							//sync config for load2 (of Type Load) 
			me->load2.Dp12 = me->flipsignDp12.Out;
							me->load2.DpeExternal = 0.1;
							//sync config for flipsignDp12 (of Type Flipsign) 
			me->flipsignDp12.In = me->tieline.Dp12;
							//sync config for tieline (of Type Tieline) 
			me->tieline.Df1 = me->gen1.Df;
							me->tieline.Df2 = me->gen2.Df;
							//sync config for print (of Type LfcPrint) 
			me->print.Df1 = me->gen1.Df;
							me->print.Df2 = me->gen2.Df;
							me->print.Dp12 = me->tieline.Dp12;
							me->print.Dpref1 = me->ic1.Dpref;
							

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Ticksource_init(&me->ticksource) != 0) {
		return 1;
	}
	if(Generator_init(&me->gen1) != 0) {
		return 1;
	}
	if(Generator_init(&me->gen2) != 0) {
		return 1;
	}
	if(IntegralController_init(&me->ic1) != 0) {
		return 1;
	}
	if(IntegralController_init(&me->ic2) != 0) {
		return 1;
	}
	if(Load_init(&me->load1) != 0) {
		return 1;
	}
	if(Load_init(&me->load2) != 0) {
		return 1;
	}
	if(Flipsign_init(&me->flipsignDp12) != 0) {
		return 1;
	}
	if(Tieline_init(&me->tieline) != 0) {
		return 1;
	}
	if(LfcPrint_init(&me->print) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* Top_syncOutputEvents() synchronises the output events of an
 * instance of Top as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncOutputEvents(Top_t  *me) {
	//first, for all cfb children, call this same function
	//then, for all connections that are connected to an output on the parent, run their run their copy
	}

/* Top_syncInputEvents() synchronises the input events of an
 * instance of Top as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncInputEvents(Top_t  *me) {
	//first, we explicitly synchronise the children
	
	me->gen1.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickGen; 
	
	me->gen1.inputEvents.event.DprefChange = me->ic1.outputEvents.event.DprefChange; 
	
	me->gen1.inputEvents.event.DpeChange = me->load1.outputEvents.event.DpeChange; 
	
	me->gen2.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickGen; 
	
	me->gen2.inputEvents.event.DprefChange = me->ic2.outputEvents.event.DprefChange; 
	
	me->gen2.inputEvents.event.DpeChange = me->load2.outputEvents.event.DpeChange; 
	
	me->ic1.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickIc; 
	
	me->ic1.inputEvents.event.DfChange = me->gen1.outputEvents.event.DfChange; 
	
	me->ic1.inputEvents.event.Dp12Change = me->tieline.outputEvents.event.Dp12Change; 
	
	me->ic2.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickIc; 
	
	me->ic2.inputEvents.event.DfChange = me->gen2.outputEvents.event.DfChange; 
	
	me->ic2.inputEvents.event.Dp12Change = me->flipsignDp12.outputEvents.event.OutChange; 
	
	me->load1.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickLoad; 
	
	me->load1.inputEvents.event.Dp12Change = me->tieline.outputEvents.event.Dp12Change; 
	
	me->load1.inputEvents.event.DpeExternalChange = 0; 
	
	me->load2.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickLoad; 
	
	me->load2.inputEvents.event.Dp12Change = me->flipsignDp12.outputEvents.event.OutChange; 
	
	me->load2.inputEvents.event.DpeExternalChange = 0; 
	
	me->flipsignDp12.inputEvents.event.InChange = me->tieline.outputEvents.event.Dp12Change; 
	
	me->tieline.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickTie; 
	
	me->tieline.inputEvents.event.DfChange = me->gen1.outputEvents.event.DfChange || me->gen2.outputEvents.event.DfChange; 
	
	me->print.inputEvents.event.Tick = me->ticksource.outputEvents.event.TickPrint; 
	
	me->print.inputEvents.event.DfChange = me->gen1.outputEvents.event.DfChange || me->gen2.outputEvents.event.DfChange; 
	
	me->print.inputEvents.event.Dp12Change = me->tieline.outputEvents.event.Dp12Change; 
	
	me->print.inputEvents.event.DprefChange = me->ic1.outputEvents.event.DprefChange || me->ic2.outputEvents.event.DprefChange; 
	

	//then, call this same function on all cfb children
	
}

/* Top_syncOutputData() synchronises the output data connections of an
 * instance of Top as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncOutputData(Top_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	
	
}

/* Top_syncInputData() synchronises the input data connections of an
 * instance of Top as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncInputData(Top_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for ticksource (of type Ticksource) which is a BFB
	
	
	//sync for gen1 (of type Generator) which is a BFB
	
	if(me->gen1.inputEvents.event.DprefChange == 1) {
		me->gen1.Dpref = me->ic1.Dpref;
		
	} 
	if(me->gen1.inputEvents.event.DpeChange == 1) {
		me->gen1.Dpe = me->load1.Dpe;
		
	} 
	
	//sync for gen2 (of type Generator) which is a BFB
	
	if(me->gen2.inputEvents.event.DprefChange == 1) {
		me->gen2.Dpref = me->ic2.Dpref;
		
	} 
	if(me->gen2.inputEvents.event.DpeChange == 1) {
		me->gen2.Dpe = me->load2.Dpe;
		
	} 
	
	//sync for ic1 (of type IntegralController) which is a BFB
	
	if(me->ic1.inputEvents.event.DfChange == 1) {
		me->ic1.Df = me->gen1.Df;
		
	} 
	if(me->ic1.inputEvents.event.Dp12Change == 1) {
		me->ic1.Dp12 = me->tieline.Dp12;
		
	} 
	
	//sync for ic2 (of type IntegralController) which is a BFB
	
	if(me->ic2.inputEvents.event.DfChange == 1) {
		me->ic2.Df = me->gen2.Df;
		
	} 
	if(me->ic2.inputEvents.event.Dp12Change == 1) {
		me->ic2.Dp12 = me->flipsignDp12.Out;
		
	} 
	
	//sync for load1 (of type Load) which is a BFB
	
	if(me->load1.inputEvents.event.Dp12Change == 1) {
		me->load1.Dp12 = me->tieline.Dp12;
		
	} 
	if(me->load1.inputEvents.event.DpeExternalChange == 1) {
		me->load1.DpeExternal = 0;
		
	} 
	
	//sync for load2 (of type Load) which is a BFB
	
	if(me->load2.inputEvents.event.Dp12Change == 1) {
		me->load2.Dp12 = me->flipsignDp12.Out;
		
	} 
	if(me->load2.inputEvents.event.DpeExternalChange == 1) {
		me->load2.DpeExternal = 0.1;
		
	} 
	
	//sync for flipsignDp12 (of type Flipsign) which is a BFB
	
	if(me->flipsignDp12.inputEvents.event.InChange == 1) {
		me->flipsignDp12.In = me->tieline.Dp12;
		
	} 
	
	//sync for tieline (of type Tieline) which is a BFB
	
	if(me->tieline.inputEvents.event.DfChange == 1) {
		me->tieline.Df1 = me->gen1.Df;
		me->tieline.Df2 = me->gen2.Df;
		
	} 
	
	//sync for print (of type LfcPrint) which is a BFB
	
	if(me->print.inputEvents.event.DfChange == 1) {
		me->print.Df1 = me->gen1.Df;
		me->print.Df2 = me->gen2.Df;
		
	} 
	if(me->print.inputEvents.event.Dp12Change == 1) {
		me->print.Dp12 = me->tieline.Dp12;
		
	} 
	if(me->print.inputEvents.event.DprefChange == 1) {
		me->print.Dpref1 = me->ic1.Dpref;
		
	} 
	
	
	//for all composite function block children, call this same function
	
	
}


/* Top_run() executes a single tick of an
 * instance of Top according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void Top_run(Top_t  *me) {
	
	
	Ticksource_run(&me->ticksource);
	
	Generator_run(&me->gen1);
	
	Generator_run(&me->gen2);
	
	IntegralController_run(&me->ic1);
	
	IntegralController_run(&me->ic2);
	
	Load_run(&me->load1);
	
	Load_run(&me->load2);
	
	Flipsign_run(&me->flipsignDp12);
	
	Tieline_run(&me->tieline);
	
	LfcPrint_run(&me->print);
	
}






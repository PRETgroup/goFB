// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Composite Function Block for Top
#include "FB_Top.h"

//When running a composite block, note that you would call the functions in this order (and this is very important)
//_preinit(); 
//_init();
//do {
//	_syncOutputEvents();
//	_syncInputEvents();
//	_syncOutputData();
//	_syncInputData();
//	_run();
//} loop;


/* Top_preinit() is required to be called to 
 * initialise an instance of Top. 
 * It sets all I/O values to zero.
 */
int Top_preinit(Top_t  *me) {
	

	
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	
	
	
	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Generator_preinit(&me->gen) != 0) {
		return 1;
	}
	
	if(IntegralController_preinit(&me->ic) != 0) {
		return 1;
	}
	
	if(LfcPrint_preinit(&me->print) != 0) {
		return 1;
	}
	
	if(Load_preinit(&me->load) != 0) {
		return 1;
	}
	
	
	

	
	
	

	

	return 0;
}

/* Top_init() is required to be called to 
 * set up an instance of Top. 
 * It passes around configuration data.
 */
int Top_init(Top_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	//sync config for gen (of Type Generator) 
			me->gen.Dpref = me->ic.Dpref;me->gen.Dpe = me->load.Dpe;//sync config for ic (of Type IntegralController) 
			me->ic.Df = me->gen.Df;//sync config for print (of Type LfcPrint) 
			me->print.Df = me->gen.Df;me->print.Dpt = me->gen.Dpt;me->print.Dph = me->gen.Dph;me->print.Dpe = me->load.Dpe;me->print.Dpref = me->ic.Dpref;//sync config for load (of Type Load) 
			

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	if(Generator_init(&me->gen) != 0) {
		return 1;
	}
	if(IntegralController_init(&me->ic) != 0) {
		return 1;
	}
	if(LfcPrint_init(&me->print) != 0) {
		return 1;
	}
	if(Load_init(&me->load) != 0) {
		return 1;
	}
	
	

	return 0;
}



/* Top_syncOutputEvents() synchronises the output events of an
 * instance of Top as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncOutputEvents(Top_t  *me) {
	//first, for all cfb children, call this same function
	//then, for all connections that are connected to an output on the parent, run their run their copy
	}

/* Top_syncInputEvents() synchronises the input events of an
 * instance of Top as required by synchronous semantics.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncInputEvents(Top_t  *me) {
	//first, we explicitly synchronise the children
	
	me->gen.inputEvents.event.Tick = 1; 
	
	me->gen.inputEvents.event.DprefChange = me->ic.outputEvents.event.DprefChange; 
	
	me->gen.inputEvents.event.DpeChange = me->load.outputEvents.event.DpeChange; 
	
	me->ic.inputEvents.event.Tick = 1; 
	
	me->ic.inputEvents.event.DfChange = me->gen.outputEvents.event.DfDptDphChange; 
	
	me->print.inputEvents.event.Tick = 1; 
	
	me->print.inputEvents.event.DfDptDphChange = me->gen.outputEvents.event.DfDptDphChange; 
	
	me->print.inputEvents.event.DpeChange = me->load.outputEvents.event.DpeChange; 
	
	me->print.inputEvents.event.DprefChange = me->ic.outputEvents.event.DprefChange; 
	
	me->load.inputEvents.event.Tick = 1; 
	

	//then, call this same function on all cfb children
	
}

/* Top_syncOutputData() synchronises the output data connections of an
 * instance of Top as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncOutputData(Top_t  *me) {
	//for all composite function block children, call this same function
	
	
	//for data that is sent from child to this CFB (me), always copy (event controlled copies will be resolved at the next level up) //TODO: arrays!?
	
	
}

/* Top_syncInputData() synchronises the input data connections of an
 * instance of Top as required by synchronous semantics.
 * It does the checking to ensure that only connections which have had their
 * associated event fire are updated.
 * Notice that it does NOT perform any computation - this occurs in the
 * _run function.
 */
void Top_syncInputData(Top_t  *me) {
	//for all basic function block children, perform their synchronisations explicitly
	
	//sync for gen (of type Generator) which is a BFB
	
	if(me->gen.inputEvents.event.DprefChange == 1) { me->gen.Dpref = me->ic.Dpref;
		
	} 
	if(me->gen.inputEvents.event.DpeChange == 1) { me->gen.Dpe = me->load.Dpe;
		
	} 
	
	//sync for ic (of type IntegralController) which is a BFB
	
	if(me->ic.inputEvents.event.DfChange == 1) { me->ic.Df = me->gen.Df;
		
	} 
	
	//sync for print (of type LfcPrint) which is a BFB
	
	if(me->print.inputEvents.event.DfDptDphChange == 1) { me->print.Df = me->gen.Df;
		me->print.Dpt = me->gen.Dpt;
		me->print.Dph = me->gen.Dph;
		
	} 
	if(me->print.inputEvents.event.DpeChange == 1) { me->print.Dpe = me->load.Dpe;
		
	} 
	if(me->print.inputEvents.event.DprefChange == 1) { me->print.Dpref = me->ic.Dpref;
		
	} 
	
	//sync for load (of type Load) which is a BFB
	
	
	
	//for all composite function block children, call this same function
	
	
}


/* Top_run() executes a single tick of an
 * instance of Top according to synchronise semantics.
 * Notice that it does NOT perform any I/O - synchronisation
 * is done using the _syncX functions at this (and any higher) level.
 */
void Top_run(Top_t  *me) {
	
	
	Generator_run(&me->gen);
	
	IntegralController_run(&me->ic);
	
	LfcPrint_run(&me->print);
	
	Load_run(&me->load);
	
}






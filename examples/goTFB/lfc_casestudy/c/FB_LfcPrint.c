// This file has been automatically generated by goFB and should not be edited by hand
// Compiler written by Hammond Pearce and available at github.com/kiwih/goFB

// This file represents the implementation of the Basic Function Block for LfcPrint
#include "FB_LfcPrint.h"

/* Arbitrary code section from SIFB */

#include <stdio.h>
#include <stdlib.h>



/* LfcPrint_preinit() is required to be called to 
 * initialise an instance of LfcPrint. 
 * It sets all I/O values to zero.
 */
int LfcPrint_preinit(LfcPrint_t  *me) {
	

	//reset the input events
	me->inputEvents.event.Tick = 0;
	//reset the input events
	me->inputEvents.event.DfDptDphChange = 0;
	//reset the input events
	me->inputEvents.event.DprefChange = 0;
	
	
	//set any input vars with default values
	
	//set any output vars with default values
	
	
	
	
	
	

	//Code provided in SIFB
	
	
	

	

	return 0;
}

/* LfcPrint_init() is required to be called to 
 * set up an instance of LfcPrint. 
 * It passes around configuration data.
 */
int LfcPrint_init(LfcPrint_t  *me) {
	//pass in any parameters on this level
	
	
	

	//perform a data copy to all children (if any present) (can move config data around, doesn't do anything otherwise)
	
	
	//Code provided in SIFB
	

	//if there are fb children (CFBs/Devices/Resources only), call this same function on them
	
	

	return 0;
}



/* LfcPrint_run() executes a single tick of an
 * instance of LfcPrint. As it is a SIFB, the execution code is provided
 */
void LfcPrint_run(LfcPrint_t  *me) {

	

	//Code provided in SIFB
	
static int tickCount = 0;
printf("%10f, %10f, %10f, %10f, %10f\r\n",
	tickCount * 0.0001,
	me->Dpref,
	me->Df,
	me->Dpt,
	me->Dph);

tickCount++;


	//Ensure input events are cleared
	me->inputEvents.event.Tick = 0;
	me->inputEvents.event.DfDptDphChange = 0;
	me->inputEvents.event.DprefChange = 0;
	
}



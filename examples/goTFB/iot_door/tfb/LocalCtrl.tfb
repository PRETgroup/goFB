serviceFB LocalCtrl;
interface of LocalCtrl {
    out event op_stay_open;
    out event op_normal;
    out event op_lock;
}

architecture of LocalCtrl {
	in "C";

	in_struct ``;
	pre_init ``;
	init ``;
	run ``;
	shutdown ``;
}